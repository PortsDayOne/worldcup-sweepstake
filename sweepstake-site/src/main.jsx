import React from "react";
import ReactDOM from "react-dom/client";
import SweepstakeDashboard from "./SweepstakeDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SweepstakeDashboard />
  </React.StrictMode>
);
